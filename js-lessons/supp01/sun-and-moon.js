// comments for my eyes only
console.log("Sun and Moon Basics");

const today = "Monday";

console.log(today);

const num1 = 200;
const num2 = 500;

const addNumbers = (x, y) => {
    // console.log(x + y);
    return x + y;
};

addNumbers(num1, num2);
addNumbers(100, 300);
const resp = addNumbers(54, 12);
console.log(resp);
